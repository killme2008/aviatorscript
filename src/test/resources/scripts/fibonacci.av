cache = seq.map();
fib = lambda(n) ->
  assert(n < 1024);
  if n <= 1 {
     return n;
  }
  
  let v = seq.get(cache, n);
  if v != nil {
    return v;
  }
  let v = fib(n - 1) + fib(n - 2);
  seq.put(cache, n, v);
  v
end;

fib(n)