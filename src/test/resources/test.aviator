m = seq.map();
fib = lambda(n) ->
  if(n <= 2) {
    return 1;
  } 
  v = seq.get(m, n);
  if(v!=nil) {
    return v;
  }
  x = fib(n - 1) + fib(n - 2);
  seq.put(m, n, x);
  x
end;

start = now();
println(fib(3));
println("cost:" + (now() - start) + " ms.");
c